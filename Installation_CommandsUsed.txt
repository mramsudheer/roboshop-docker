https://github.com/mramsudheer/roboshop-docker.git
git diff --stat origin/main main

# DISK RESIZE

df -hT
vgs - To see total Unallocated space
lvs - Each partition size
lsblk
sudo growpart /dev/nvme0n1 4
sudo lvextend -L +20G /dev/RootVG/rootVol
sudo lvextend -L +30G /dev/RootVG/varVol
sudo xfs_growfs /
sudo xfs_growfs /var
#----------------------------------------------------------------------------

# DOCKER INSTALLATION

sudo dnf -y install dnf-plugins-core
sudo dnf config-manager --add-repo https://download.docker.com/linux/rhel/docker-ce.repo
sudo dnf install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker ec2-user
logout and login again

docker commands
===============
docker ps -> running containers
docker images -> images available
docker pull nginx -> pull the image from dockerhub
docker create nginx -> creates the containers, status is created
docker start container-id -> start the container
docker rm container-id
docker rmi nginx
docker run nginx = pull + create + start

nginx = OS + system packages + nginx software
============================================================
docker build -t mramsudheer/roboshop:mongodb-1.0.0 .
docker images
docker create --name mongodb mramsudheer/roboshop:mongodb-1.0.0
docker ps
docker start monogdb

docker login -u mramsudheer

docker tag mongodb:1.0.0 mramsudheer/roboshop:mongodb-1.0.0
docker push mramsudheer/roboshop:mongodb-1.0.0
docker pull mramsudheer/roboshop:mongodb-1.0.0

docker push mramsudheer/roboshop:payment-1.1.0 && \
docker push mramsudheer/roboshop:shipping-1.1.0 && \
docker push mramsudheer/roboshop:user-1.1.0


docker tag catalogue:1.0.0 mramsudheer/roboshop:catalogue-1.0.0
docker push mramsudheer/roboshop:catalogue-1.0.0

TO CREATE A CONTAINER
docker create --name mongodb mongodb:1.0.0

TO START THE CONTAINER
docker start mongodb

TO LOGIN INTO CONTAINER
docker exec -it maongodb bash

LIST OF NETWORKS
docker network ls

TO CHECK THE CONTAINERS NETWORK
docker inspect -f '{{.Name}} - {{range .NetworkSettings.Networks}}{{.NetworkID}}{{end}}' $(docker ps -q)

TO STOP ALL CONTAINERS
docker stop $(docker ps -q)

TO DELETE THE CONTAINER
docker rm mongodb
docker rm -f monogdb

TO DELETE AN IMAGE
docker rmi roboshop:catalogue-1.0.0

TO DELETE ALL IMAGES
docker rmi -f $(docker images -q)

TO DELETE IMAGE FROM DOCKER HUB
sudo dnf install -y skopeo
skopeo delete --creds your_username:your_password \
  docker://docker.io/mramsudheer/roboshop:mongodb-1.0.0

skopeo delete --creds mramsudheer:Saibaba06! \
  docker://docker.io/mramsudheer/roboshop:cart-1.0.0

TO RECLAIM DISK SPACE BY DELETING ALL UNUSED DOCKER DATA AT ONCE
docker system prune -f

Complete System Wipe
docker system prune -a --volumes

WRITE EVERYTHING IN docker-compose.yaml AND RUN
docker compose up -d --build
docker compose ps

docker compose logs -f frontend
docker compose logs frontend

docker compose down
docker network inspect roboshop

docker compose up -d redis
docker compose restart user


ACCESS with PUBLIC IP